# 🐰 KooJai (คู่ใจ) - AI เพื่อนฟังใจนักเรียน

ยินดีต้อนรับสู่โปรเจกต์ **KooJai**! นี่คือเว็บแอปพลิเคชัน AI ที่สร้างขึ้นเพื่อเป็นเพื่อนรับฟังนักเรียน โดยใช้เทคโนโลยีเสียงสนทนาแบบ Real-time (Gemini Multimodal Live API)

โปรเจกต์นี้เหมาะสำหรับผู้เริ่มต้นที่อยากเรียนรู้การเขียน **React**, **TypeScript** และการต่อ **AI API**

---

## 🌟 ฟีเจอร์หลัก
1.  **Voice Chat**: คุยกับ AI ด้วยเสียงภาษาไทย แบบโต้ตอบทันที (Low latency)
2.  **Emotional Support**: AI มีบุคลิกเป็นพี่กระต่ายใจดี รับฟังปัญหาวัยรุ่น
3.  **Visualizer**: กราฟิกวงกลมเต้นตามจังหวะเสียงพูด
4.  **Analysis & Report**: วิเคราะห์ความเสี่ยง (Safety Risk) และสรุปรายงานให้ครูแนะแนว
5.  **Long-term Memory**: AI จำบริบทการคุยครั้งก่อนได้ (เช่น จำได้ว่าพรุ่งนี้มีสอบ)

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
*   **Frontend**: React 19, TypeScript, Tailwind CSS
*   **Build Tool**: Vite
*   **AI Model**: Google Gemini 2.5 Flash (Live & GenerateContent)
*   **Database**: IndexedDB (เก็บข้อมูลในเบราว์เซอร์ ไม่ต้องมี Server Database)

---

## 🔐 การจัดการ API Key สำหรับใช้งานจริง (Production)

ตอนที่ทดลองเล่น เรามักจะกรอก API Key ผ่านหน้าเว็บ (ซึ่งจะเก็บแค่ในเครื่องเรา) แต่ถ้าเราจะเอาไปให้นักเรียนใช้จริง เราคงไม่อยากให้นักเรียนต้องไปสมัคร Key เองใช่ไหมครับ?

เราสามารถฝัง Key ไว้ในระบบได้เลย โดยใช้วิธี **Environment Variables**

### วิธีทำ:
1.  สร้างไฟล์ชื่อ `.env` ที่โฟลเดอร์นอกสุดของโปรเจกต์ (ระดับเดียวกับ `package.json`)
2.  ใส่เนื้อหาดังนี้:
    ```
    VITE_GEMINI_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    ```
    (เปลี่ยน `AIzaSy...` เป็น Key ของคุณเอง)
3.  บันทึกไฟล์ แล้วสั่งรันโปรแกรมใหม่ (`npm run dev`)
4.  หน้าเว็บจะข้ามหน้ากรอก Key แล้วพาไปหน้า Login ทันที!

### ⚠️ คำเตือนเรื่องความปลอดภัย (สำคัญมาก!)
*   การใส่ Key ใน `.env` แบบนี้ ตัว Key จะถูกฝังไปกับโค้ดหน้าเว็บ ใครที่มีความรู้คอมพิวเตอร์สามารถแอบดู Key นี้ได้ (Inspect Code)
*   **สำหรับโปรเจกต์นักเรียน/Demo:** วิธีนี้ยอมรับได้ แต่ควรกำหนด "Budget Limit" ใน Google Cloud Console เพื่อป้องกันคนเอาไปใช้จนเงินหมด
*   **สำหรับระบบจริงจัง (Commercial):** ห้ามทำแบบนี้! คุณต้องสร้าง **Backend Server** (API Proxy) เพื่อซ่อน Key ไว้ที่ Server แล้วให้หน้าเว็บเรียกผ่าน Server เท่านั้น

---

## 🚀 วิธีการติดตั้งและรันโปรเจกต์ (Setup Guide)

### สิ่งที่ต้องมีก่อน (Prerequisites)
1.  **Node.js**: โปรแกรมสำหรับรัน JavaScript [ดาวน์โหลดที่นี่](https://nodejs.org/) (แนะนำเวอร์ชั่น LTS)
2.  **Google AI Studio API Key**: กุญแจสำหรับใช้ AI [กดรับฟรีที่นี่](https://aistudio.google.com/app/apikey)

### ขั้นตอน (Step-by-Step)

1.  **ดาวน์โหลดโปรเจกต์**
    เปิด Terminal แล้วพิมพ์คำสั่ง:
    ```bash
    git clone <your-repo-url>
    cd koojai-app
    ```

2.  **ลง Library ต่างๆ (Install Dependencies)**
    ```bash
    npm install
    ```

3.  **รันโปรแกรม (Start Development Server)**
    ```bash
    npm run dev
    ```

4.  **เปิดเว็บ**
    คลิก Link ที่ขึ้นใน Terminal (มักจะเป็น `http://localhost:5173`)

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

นี่คือแผนที่ของโปรเจกต์นี้ เพื่อให้รู้ว่าไฟล์ไหนอยู่ตรงไหนและทำหน้าที่อะไรครับ

```
koojai-app/
├── 📁 src/                  # โฟลเดอร์เก็บโค้ดหลักที่เราเขียน (Source Code)
│   ├── 📁 components/       # เก็บชิ้นส่วนหน้าจอ (UI Components)
│   │   └── Visualizer.tsx   # กราฟิกวงกลมเต้นตามเสียง
│   ├── 📁 services/         # เก็บตัวติดต่อกับโลกภายนอก (API)
│   │   └── geminiService.ts # ตัวจัดการเชื่อมต่อกับ Google Gemini AI (สำคัญมาก!)
│   ├── 📁 utils/            # เก็บฟังก์ชันเครื่องมือช่วยเหลือ (Utilities)
│   │   ├── audioUtils.ts    # ช่วยแปลงไฟล์เสียง (Encode/Decode)
│   │   └── db.ts            # ช่วยบันทึกข้อมูลลง Database ในเครื่อง
│   ├── App.tsx              # หน้าจอหลักของแอพ รวมทุกอย่างไว้ที่นี่
│   ├── index.tsx            # จุดเริ่มต้น (Entry Point) ที่เอา App ไปแปะใน HTML
│   └── types.ts             # สมุดจดประเภทข้อมูล (TypeScript Interfaces)
│
├── 📁 public/               # เก็บไฟล์นิ่งๆ ที่ไม่ต้องแก้โค้ด (รูปภาพ, ไอคอน)
├── index.html               # หน้าเว็บหลัก (โครงกระดูกของเว็บ)
│
├── ⚙️ ไฟล์ Config (ตั้งค่าระบบ)
├── package.json             # "บัตรประชาชน" ของโปรเจกต์ (บอกชื่อ, เวอร์ชั่น, ของที่ต้องใช้)
├── vite.config.ts           # "โรงงานผลิต" (ตั้งค่าการ Build เว็บ)
├── tsconfig.json            # "กฎไวยากรณ์" (ตั้งค่า TypeScript)
├── tailwind.config.js       # "ดีไซเนอร์" (ตั้งค่า Theme สี, ฟอนต์ ของ CSS)
└── postcss.config.js        # "ผู้ช่วยดีไซเนอร์" (ตัวช่วยประมวลผล CSS)
```

---

## 📚 อธิบายไฟล์ Config (สำหรับมือใหม่)

น้องๆ อาจจะงงว่าไฟล์พวกนี้คืออะไร ทำไมต้องมี? เปรียบเทียบง่ายๆ ดังนี้ครับ:

1.  **`package.json`**: เปรียบเหมือน **"ใบสั่งอาหาร"**
    *   มันบอกว่าโปรเจกต์นี้ต้องใช้วัตถุดิบ (Library) อะไรบ้าง เช่น "ขอ React จานนึง", "ขอ Tailwind จานนึง"
    *   เมื่อเราพิมพ์ `npm install` พ่อครัว (NPM) จะไปซื้อของตามใบสั่งนี้มาให้เรา

2.  **`vite.config.ts`**: เปรียบเหมือน **"เครื่องจักรในโรงงาน"**
    *   Browser (Chrome/Safari) อ่านภาษา TypeScript (`.tsx`) โดยตรงไม่ออก
    *   Vite คือเครื่องจักรที่ช่วยแปลภาษาพวกนี้ให้กลายเป็น JavaScript ธรรมดาที่ Browser เข้าใจ และช่วยให้เรารันเว็บดูได้ไวๆ

3.  **`tailwind.config.js`**: เปรียบเหมือน **"สมุดรวมสีประจำโรงเรียน"**
    *   เอาไว้กำหนดว่า สีหลักของเว็บคือสีอะไร ฟอนต์ชื่ออะไร เพื่อให้ทั้งเว็บสวยไปในทิศทางเดียวกัน

---

## 💡 Advanced: ถ้าอยากต่อ Database จริงๆ ต้องทำยังไง?

ตอนนี้โปรเจกต์ใช้ **IndexedDB** ซึ่งเก็บข้อมูลไว้ใน "คอมพิวเตอร์ของผู้ใช้เอง" (เหมือนสมุดไดอารี่ส่วนตัว) ข้อดีคือฟรีและง่าย แต่ข้อเสียคือถ้าเปลี่ยนเครื่อง ข้อมูลก็หาย และครูก็ดู Report ของนักเรียนไม่ได้จริงๆ

ถ้าจะทำเป็นระบบจริงให้ครูดูข้อมูลได้ (Production Grade) ต้องทำดังนี้:

### 1. ต้องมี "Backend Server"
เราต้องสร้างคอมพิวเตอร์เครื่องกลาง (Server) เพื่อรับฝากข้อมูล
*   **ตัวเลือกยอดนิยม:** Node.js (Express), Python (FastAPI/Django), หรือ Go
*   **แบบง่าย (แนะนำ):** ใช้ **Firebase** หรือ **Supabase** (ไม่ต้องสร้าง Server เอง มี Database ให้ใช้เลย)

### 2. เปลี่ยนโค้ดใน `utils/db.ts`
จากเดิมที่สั่งให้บันทึกลง IndexedDB เราต้องเปลี่ยนเป็นสั่งให้ส่งข้อมูลไป Server แทน

**ตัวอย่างโค้ด (สมมติว่ามี Server แล้ว):**

```typescript
// utils/db.ts (แบบใหม่)

// แทนที่จะบันทึกลงเครื่อง...
// export const saveReport = async (report: TeacherReport) => { ...indexedDB logic... }

// เปลี่ยนเป็นส่งไป Server
export const saveReport = async (report: TeacherReport) => {
  try {
    const response = await fetch('https://my-api-server.com/api/reports', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        // 'Authorization': 'Bearer ...' // อาจต้องมีการล็อกอิน
      },
      body: JSON.stringify(report)
    });
    
    if (!response.ok) throw new Error('Save failed');
    
  } catch (error) {
    console.error("ส่งข้อมูลไป Server ไม่สำเร็จ", error);
  }
};
```

### 3. สร้างหน้า Dashboard สำหรับครู
ทำหน้าเว็บอีกหน้านึง ให้ครูมาล็อกอิน แล้วดึงข้อมูลจาก Server (`GET /api/reports`) มาแสดงเป็นกราฟหรือตาราง

---

## 💬 คำแนะนำสุดท้าย
การเขียนโปรแกรมคือการ "ลองผิดลองถูก" อย่ากลัวที่จะแก้โค้ดแล้วพัง เพราะนั่นคือตอนที่เราจะได้เรียนรู้มากที่สุดครับ

ขอให้สนุกกับการสร้าง **KooJai** นะครับ! ✌️🐰